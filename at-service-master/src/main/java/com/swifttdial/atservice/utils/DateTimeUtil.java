package com.swifttdial.atservice.utils;

/**
 * Created by gathigai on 9/8/16.
 */
/*public class DateTimeUtil {
    private static SimpleDateFormat format = new SimpleDateFormat();

    public static String dateToString(Date date) {
        format = new SimpleDateFormat("dd-M-yyyy hh:mm:ss");
        return format.format(date);
    }

    public static Date zonedDateTimeToDate(ZonedDateTime zonedDateTime) {
        return Date.from(zonedDateTime.toInstant());
    }

    public static String zonedDateTimeToString(ZonedDateTime zonedDateTime) {
        return dateToString(zonedDateTimeToDate(zonedDateTime));
    }

    public static ZonedDateTime toZonedDateTime(Date utilDate) {
        if (utilDate == null) {
            return null;
        }
        final ZoneId systemDefault = ZoneId.systemDefault();
        return ZonedDateTime.ofInstant(utilDate.toInstant(), systemDefault);
    }
}*/
